# 솔루션

## 통과한 코드

### 1.
```java
import java.util.*;

class Solution {
    public int solution(int k, int[] tangerine) {
        int answer = 0;
        Map<Integer, Integer> map = new HashMap<>();
        int total = tangerine.length;
        for (int i = 0; i < total; i++)
            map.put(tangerine[i], map.getOrDefault(tangerine[i], 0) + 1);
        
        List<Map.Entry<Integer, Integer>> entries = new LinkedList<>(map.entrySet());
        Collections.sort(entries, (o1, o2) -> o1.getValue().compareTo(o2.getValue()));

        LinkedHashMap<Integer, Integer> result = new LinkedHashMap<>();
        for (Map.Entry<Integer, Integer> entry : entries) {
            result.put(entry.getKey(), entry.getValue());
        }
        int size = result.keySet().size();
        
        for (int key : result.keySet()) {
            total -= result.get(key);
            size--;
            if (total == k) {
                return size;
            }
            else if(total < k) {
                return size + 1;
            }
        }
        return answer;
    }
}
```

## 풀이 해설

이 문제 전에 풀었었나봐요 ㅎㅎ 들어가니까 코드가 있네요.
1. map 에 요소들 다 집어넣습니다.
2. value값으로 정렬합니다. (내림차순)
3. 첫번째 부터 집어넣으면 가짓수가 최소화 됩니다.


